{
  "license": "MIT",
  "scripts": {
    "debug": "yarn start:local",
    "build": "tsc",
    "start": "NODE_PATH=./dist NODE_ENV=production node dist",
    "start:local": "NODE_PATH=src ts-node-dev --files --inspect --respawn -- src/index.ts",
    "test": "jest",
    "test:watch": "jest --watch --passWithNoTests"
  },
  "devDependencies": {
    "@types/dedent": "^0.7.0",
    "@types/jest": "^27.4.1",
    "@types/node": "^17.0.21",
    "dedent": "^0.7.0",
    "jest": "^27.5.1",
    "ts-jest": "^27.1.4",
    "ts-node-dev": "^1.1.8",
    "typescript": "^4.6.3"
  }
}
